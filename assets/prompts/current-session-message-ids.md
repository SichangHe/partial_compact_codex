Current-session message IDs for `partial_compact` range selection.

Use these current-session `msg...` IDs as `from_message_id` and `to_message_id` endpoints when calling `partial_compact`.

{{MESSAGE_ID_LINES}}
